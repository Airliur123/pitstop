# Local Docker infrastructure

`compose.yaml` at the repository root is the executable definition. Application containers are
intentionally absent in Phase 0 so hot reload runs directly inside WSL 2.
